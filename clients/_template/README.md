# Client template

Create one folder per client under `clients/`, then create one folder per platform inside that client folder.

Example:

```text
clients/example-client/pinterest/
clients/example-client/amazon/
clients/example-client/meta/
```
